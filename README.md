# Editor

View and edit every file on your Mobius, with git status and an agent that makes changes you can watch.

A Möbius mini-app. Install it from the in-app App Store, or:

```
POST /api/apps/install { "manifest_url": "https://raw.githubusercontent.com/mobius-os/app-editor/main/mobius.json" }
```
